# --------------------------------------------------------------------- #
##### Functions to export datasets for the {ppl.tits} package users #####
# --------------------------------------------------------------------- #


ntits_clean <- ppl.tits::tdata_upD_final()$clean_dataset # My functions that creates the R object I want to
# export and made available for the package users.
usethis::use_data(ntits_clean, overwrite = TRUE) # Creates/updates data/ntits_clean.rda and, the first time
# it is run, modifies the DESCRIPTION file to add `LazyData: true` and `Depends: R (>= 2.10)`!



### To document my dataset (SHOULD ONLY BE RUN ONCE!!!) ###
### ------------------------------------------------------ ###


# IMPORTANT NOTE: as this is a workflow code and is thus "off-package", it should be manually updated
# as it won't be automatically by running the package's functions or the {targets} pipeline (see
# chapter 4. below).
# OR WON'T IT?????????????????????????????????????????????????????????????????????????
# OR WON'T IT?????????????????????????????????????????????????????????????????????????
# OR WON'T IT?????????????????????????????????????????????????????????????????????????
# OR WON'T IT????????????????????????????????????????????????????????????????????????? --> I can create a custom function
# within the _targets.R file to define a special target that creates/updates this dataset, and thus use it in reports or
# for development, and thus delete "clean_tdata"??????????????????
